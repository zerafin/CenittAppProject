<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.cenitt.www.cenittapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoconfondo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioSesion" />
        <!-- android:label="This is the Hello World Application" -->
        <activity
            android:name=".RegistroUsuario"
            android:parentActivityName=".InicioSesion">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".InicioSesion" />
        </activity>
        <activity android:name=".InformacionDatos" />
        <activity android:name=".InformacionHerramientas" />
        <activity android:name=".OpcionTarifas" />
        <activity android:name=".Tarifa_03" />
        <activity android:name=".TarifaOM" />
        <activity android:name=".TarifaHM" />
        <activity android:name=".TarifaBasica"></activity>
    </application>

</manifest>